# API hooks

Hook khusus untuk state pemanggilan API di client, misalnya `use-children.ts`, `use-growth-records.ts`, dan `use-posyandus.ts`.

Query database di Server Component atau Route Handler tidak ditempatkan di sini.
